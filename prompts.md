## Part A — a_fare.js
**Prompt:** เขียนฟังก์ชันคำนวณค่ารถให้หน่อย ต่อมาระบุรายละเอียดว่าเป็นแอปรถ NGV ในมหาวิทยาลัย: 2 กม.แรก 10 บาท กม.ถัดไปคิด กม.ละ 2 บาท เศษของกิโลเมตรปัดขึ้น ระยะทางติดลบหรือไม่ใช่ตัวเลขให้คืน 0 ใช้ ES6 arrow function ไม่ใช้ library ภายนอก มี comment ภาษาไทยสั้น ๆ และให้ `console.log` ทดสอบ `1.5`, `2`, `7.2` รวม 3 กรณี
**ผลลัพธ์ย่อ:** ได้ฟังก์ชัน `calcFare(distanceKm)` และการทดสอบ 3 กรณีตามรูปแบบที่ขอ
**ตรวจแล้ว:** ใช้ได้ — ตรวจชนิดข้อมูล, ค่าติดลบ, `NaN` และ `Infinity`; ใช้ `Math.ceil` ปัดเศษกิโลเมตรขึ้น
**รอบที่ iterate:** 2 — รอบแรกเพิ่มตัวอย่างทดสอบหลายกรณี ก่อนปรับให้ตรงกับ prompt ล่าสุด

## Part B — b_review.md
**Prompt:** ให้ตรวจโค้ด `team` ในฐานะ senior developer บอกจุดที่ควรปรับปรุง 3 ข้อ เรียงตามความสำคัญ พร้อมเหตุผลและโค้ดที่แก้แล้ว จากนั้นเลือกคำแนะนำ 1 ข้อที่เห็นด้วยและเข้าใจ นำไปแก้ในโค้ดจริง รันซ้ำให้ผลเท่าเดิม และบันทึกว่าแก้อะไร เพราะอะไร และข้อไหนที่ไม่เอา เพราะอะไร
**ผลลัพธ์ย่อ:** เลือกข้อ 1 คือการตรวจสอบข้อมูลสมาชิกทีม เพิ่มการตรวจชื่อไม่เป็นค่าว่างและ `tasksDone` เป็นจำนวนเต็มไม่ติดลบ
**ตรวจแล้ว:** ใช้ได้ — ข้อมูลเดิมผ่านการตรวจและแสดงผล `true`; ข้อ 2 และ 3 ไม่ทำซ้ำ เพราะมี `ROLES` และ `Object.freeze` อยู่แล้ว
**รอบที่ iterate:** 2 — รอบแรกรีวิว 3 ข้อ รอบที่สองเลือก 1 ข้อไปแก้ในโค้ดจริง

## Part C — c_debug.js
**Prompt:** จากผลลัพธ์จริง `สายที่มาสาย: []` และ `ผู้โดยสารรวม: [object Object]6238` ให้อธิบายสาเหตุก่อน แล้วค่อยให้โค้ดที่แก้ โดยอย่าเปลี่ยนส่วนที่ไม่เกี่ยว
**ผลลัพธ์ย่อ:** พบว่า `filter` ไม่มี `return` จึงได้ `[]` และ `reduce` ไม่มีค่าเริ่มต้น `0` จึงนำ object ตัวแรกไปบวกกับตัวเลข เกิด `[object Object]6238`
**ตรวจแล้ว:** แก้เฉพาะจุดที่เกี่ยวข้องแล้ว ผลที่ได้ตรงความคาดหวัง: `['NGV-2', 'NGV-3']` และ `145`
**รอบที่ iterate:** 1 — แก้ `filter` ให้คืนค่าเงื่อนไข และเพิ่มค่าเริ่มต้น `0` ให้ `reduce`

## Part D — d_story.js
**Prompt:** เขียนฟังก์ชัน JavaScript ล้วนที่ทำงานกับ array ของ object สำหรับรายการงาน Work and Travel ในอเมริกา เมืองใกล้นิวยอร์ก ใช้ USD ต้องมีค่าแรงต่อชั่วโมง รายได้ต่อเดือน ค่าเช่าบ้าน เงินคงเหลือหลังหักค่าใช้จ่าย และเงื่อนไขต่าง ๆ ไม่เกิน 20 บรรทัด พร้อมข้อมูลจำลองและ `console.log` 3 กรณี รวมกรณีขอบ: ไม่มีงานตรงเงื่อนไข และค่าเช่าแพงกว่ารายได้ ก่อนเขียนให้ถาม 3 คำถามที่จำเป็น
**ผลลัพธ์ย่อ:** หลังตอบคำถามเรื่องพื้นที่ สกุลเงิน และกรณีขอบ ได้ `findJobs` ที่ใช้ `filter` และ `map` คำนวณรายได้ต่อเดือนกับเงินคงเหลือ แล้วถูกย้ายมาไว้ใน `d_story.js`
**ตรวจแล้ว:** ใช้ได้ — มีกรณีปกติ, ผลลัพธ์ว่างเมื่อไม่มีงานตรงเงื่อนไข และผลลัพธ์ `remaining` ติดลบเมื่อค่าเช่าสูงกว่ารายได้
**รอบที่ iterate:** 2 — รอบแรกเพิ่มโค้ดใน `prompts.md` รอบที่สองย้ายโค้ดทั้งชุดไป `d_story.js`


